apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  namespace: services
  name: account-app-virtual-service
  labels:
    env: {{ .Values.global.env }}
spec:
  exportTo:
    - "."
  hosts:
    - account-app
  gateways:
    - mesh
  http:
    - match:
        - port: {{ .Values.service.listenPort }}
      route:
        - destination:
            host: account-app-service.services
            port:
              number: {{ .Values.service.listenPort }}
