#============
# General
#============
general:
  env: prd
#============
# Kubernetes
#============
kubernetes:
  image:
    account:
      gin: latest
    customer:
      fastapi: "933cfde"
    orchestrator:
      fastapi: latest
    order:
      lumen: latest
      nginx: latest
