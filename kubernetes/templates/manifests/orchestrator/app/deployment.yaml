apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: microservices
  name: orchestrator-pod
  labels: 
    app: orchestrator
    env: {{ .Values.labels.env }}
spec:
  replicas: 1
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 0%
  selector:
    matchLabels:
      app: orchestrator
      env: {{ .Values.labels.env }}
  template:
    metadata:
      labels:
        app: orchestrator
        env: {{ .Values.labels.env }}
    spec:
{{- if eq .Values.labels.env "dev" }}
  {{- include "dev.orchestrator.app.deployment" . }}
{{- end }}